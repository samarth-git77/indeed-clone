pipeline {
    agent any

    tools {
        maven "Maven"
    }

    stages {

        stage('Clone') {
            steps {
                echo 'Cloning repo...'
            }
        }

        stage('Build') {
            steps {
                dir('server') {
                    sh 'mvn clean package'
                }
            }
        }

    }
}