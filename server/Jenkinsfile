pipeline {
    agent any

    tools {
        maven "Maven"
    }

    stages {

        stage('Build') {
            steps {
                echo 'Building without tests...'
                dir('server') {
                    bat 'mvn clean package -DskipTests'
                }
            }
        }

    }
}