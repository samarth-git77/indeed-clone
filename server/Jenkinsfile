pipeline {
    agent any

    tools {
        maven "Maven"
    }

    stages {

        stage('Build') {
            steps {
                echo 'Starting build...'
                dir('server') {
                    bat 'mvn clean package'
                }
            }
        }

    }
}